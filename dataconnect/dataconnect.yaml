specVersion: "v1"
serviceId: "smulib"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "smulib-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
